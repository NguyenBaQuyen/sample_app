<li>
  <%= link_to user.name + " " + user.gender, user %>
  <% if current_user.admin? && !current_user.current_user?(user) %>
    | <%= link_to "delete", user, method: :delete,
      data: { confirm: "You sure?" } %>
  <% end %>
</li>
